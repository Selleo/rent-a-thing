<h2>All bookings</h2>

<table>
  <tr>
    <th>Customer</th>
    <th>Starts</th>
    <th>Ends</th>
    <th>Archived at</th>
  </tr>

  <%= render partial: 'booking', collection: @bookings %>

  <%# @bookings.each do |booking| %>
    <%#= render partial: "booking", booking: booking %>
  <%# end %>
</table>
